[project]
name = "linear-regression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tezy Apps", email = "tezyapps@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
linear-regression = "linear_regression:main"
hwp = "linear_regression:main"
logistic-regression = "logistic_regression:main"
log-reg = "logistic_regression:main"
hp = "height_predictor:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
